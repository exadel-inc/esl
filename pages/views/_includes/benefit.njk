<div class="content container benefit">
  <h1 class="benefit-title">
    Why the ESL Web Components <br> Library?
  </h1>
  <div class="benefit-container">
    {% for item in benefit.benefit %}
      <div class="benefit-item">
        <div class="benefit-icon">
         {% include "static/assets/" + item.icon %}
        </div>
        <div class="benefit-content-name">{{ item.name }}</div>
        <p class="benefit-content-text">
        {{ item.text }}
        </p>
      </div>
    {% endfor %}
  </div>
    {% include "static/assets/common/line.svg" %}
</div>

