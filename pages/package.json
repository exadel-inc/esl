{
  "name": "exadel-smart-library-samples",
  "version": "1.0.0",
  "description": "Samples static server for ESL documentation, playground and local development",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently --kill-others \"npm run start\" \"npm run watch\"",
    "start": "node server.js",
    "watch": "gulp watchLocal",
    "build": "gulp buildLocal",
    "build-and-start": "npm run build && npm run start",
    "build-pages": "npm run build-pages:pages && npm run build-pages:assets",
    "build-pages:pages": "node build-pages.js",
    "build-pages:assets": "cpx static/**/*.{js,css} ../docs/"
  },
  "sideEffects": [
    "src/common/*.ts",
    "src/polyfill-light.ts",
    "src/polyfill-full.ts"
  ],
  "private": true,
  "license": "MIT"
}
