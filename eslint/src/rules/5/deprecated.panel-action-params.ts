import {buildRule} from '../../core/deprecated-alias';

/**
 * Rule for deprecated `PanelActionParams` alias for {@link ESLPanelActionParams}
 */
export default buildRule({
  alias: 'ESLPanelActionParams',
  deprecation: 'PanelActionParams'
});
