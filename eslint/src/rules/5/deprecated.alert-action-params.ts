import {buildRule} from '../../core/deprecated-alias';

/**
 * Rule for deprecated `AlertActionParams` alias for {@link ESLAlertActionParams}
 */
export default buildRule({
  alias: 'ESLAlertActionParams',
  deprecation: 'AlertActionParams'
});
