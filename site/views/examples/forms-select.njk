---
layout: content
title: Forms - Select Dropdown
seoTitle: Custom select dropdown examples based on ESL web components
name: Select Dropdown
tags: [examples, playground]
icon: examples/select-dropdown.svg
aside:
components:
- esl-select
---

<section class="row">
  <div class="col-12">
    <style>
      .form {
        display: block;
        width: 100%;
      }
    </style>
    <uip-root>
      <script type="text/html"
              label="Custom"
              uip-snippet
              uip-isolated
              uip-snippet-js="js-snippet-select-registration">
        <form class="form bg-white border p-4" style="min-height: 250px">
          <div class="form-group">
            <label for="country_field" class="form-label">Please select your country: </label>
            <esl-select style="display: block"
                        placeholder="Country"
                        select-all-label="Select All Countries">
              <select esl-select-target
                      id="country_field"
                      name="country_field"
                      class="form-control">
                <option class="disabled" value="USA">United States</option>
                <option value="CAN">Canada</option>
                <option value="GBR">United Kingdom</option>
                <option value="AUS">Australia</option>
                <option value="DEU">Germany</option>
                <option value="FRA">France</option>
                <option value="ESP">Spain</option>
                <option value="ITA">Italy</option>
                <option value="CHN">China</option>
                <option value="JPN">Japan</option>
                <option value="BRA">Brazil</option>
                <option value="IND">India</option>
                <option value="MEX">Mexico</option>
                <option value="ZAF">South Africa</option>
                <option value="ARG">Argentina</option>
                <option value="SWE">Sweden</option>
                <option value="NOR">Norway</option>
                <option value="POL">Poland</option>
              </select>
            </esl-select>
          </div>
          <div class="form-group">
            <button class="btn btn-sec-blue">Submit</button>
            <button type="reset" class="btn btn-sec-orange">Reset</button>
          </div>
          <div class="demo-info panel panel-info">
          </div>
        </form>
      </script>
      <script id="js-snippet-select-registration" type="text/plain">
  import {ESLSelect, ESLScrollbar} from '@exadel/esl';
  ESLSelect.register();
  ESLScrollbar.register();

  function updateFormInfo(event) {
    const form = document.querySelector('.form');
    const info = document.querySelector('.demo-info');

    event.preventDefault();

    const data = {};
    const formData = new FormData(form);

    formData.forEach((value, key) => {
      if (data[key]) {
        data[key] = [].concat(data[key], value);
      } else {
        data[key] = value;
      }
    });

    info.textContent = JSON.stringify(data, null, 2);
    info.removeAttribute('hidden');
  }

  document.addEventListener('submit', updateFormInfo);
   </script>

      <uip-snippets class="uip-toolbar" dropdown-view="@xs"></uip-snippets>
      <uip-preview></uip-preview>
      <uip-settings target="esl-select" label="Settings" resizable vertical="@+sm">
        <uip-bool-setting label="Pin-selected" attribute="pin-selected"></uip-bool-setting>
        <uip-bool-setting label="Multiple" attribute="multiple" target="select"></uip-bool-setting>
        <uip-bool-setting label="Disabled" attribute="disabled" target=".disabled"></uip-bool-setting>
      </uip-settings>
      <uip-editor label="Source code (HTML)" collapsible copy></uip-editor>
      <uip-editor source="js" label="Source code (JS)" collapsible collapsed copy></uip-editor>
    </uip-root>
  </div>
</section>
