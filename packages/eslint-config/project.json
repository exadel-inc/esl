{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "pack": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm pack --pack-destination=../../target/",
        "cwd": "packages/eslint-config"
      },
      "cache": true,
      "outputs": [
        "{workspaceRoot}/target/exadel-eslint-config-esl-*.tgz"
      ]
    }
  }
}
