---
title: Autoplay Carousel (CSS)
order: 11
tags: carousel-sample
---

<div class="esl-carousel-no-extra-space esl-carousel-nav-container">
  <button type="button" class="esl-carousel-arrow inner prev hide-xs" esl-carousel-nav="group:prev">
    <span class="sr-only">Previous Slide</span>
  </button>

  <esl-carousel id="carousel-autoplay"
                demo-options-target
                type="css-slide"
                esl-carousel-autoplay="{duration: '10s', control: '.autoplay-ctrl', controlCls: 'btn-sec-blue !btn-sec-orange'} | @XS => none"
                loop="true">
    <ul esl-carousel-slides>
      {% for i in range(0, 4) -%}
        <li esl-carousel-slide {{ 'active' if loop.first }}>
          <div class="card">
            <div class="img-container img-container-16-9" esl-image-container>
              <img class="img-fade img-cover"
                   alt="{{ 'Carousel slide ' + loop.index }}"
                   src="{{ '/assets/carousel/' + loop.index + '-sm.jpg' | url }}"
                   loading="lazy" />
              <div class="h1 text-slide text-white">Slide {{ loop.index }}</div>
            </div>
          </div>
        </li>
      {%- endfor %}
    </ul>
  </esl-carousel>
  <div class="d-flex justify-content-center p-2">
    <button class="autoplay-ctrl btn btn-sec-orange">
      Toggle Autoplay
      <svg xmlns="http://www.w3.org/2000/svg"
           class="progress"
           viewBox="0 0 24 24"
           width="24" height="24"
           esl-carousel-autoplay-progress>
        <circle class="progress" r="10" cx="12" cy="12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></circle>
        <style>
          .progress circle {
            stroke-dasharray: 62.83185307179586; /* 2 * Math.PI * 10 */
            stroke-dashoffset: 62.83185307179586; /* full circle */
            transform: rotate(-90deg);
            transform-origin: center;
          }
          .progress[animate] circle {
            transition: stroke-dashoffset var(--esl-autoplay-timeout, 5s) linear;
            stroke-dashoffset: 0;
          }
        </style>
      </svg>
    </button>
  </div>

  <button type="button" class="esl-carousel-arrow inner next hide-xs" esl-carousel-nav="group:next">
    <span class="sr-only">Next Slide</span>
  </button>

  <style>
    .text-slide {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
    }
  </style>
</div>
